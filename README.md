# verilog_memory_project
This project implements and verifies a simple memory module using Verilog. The objective of the project is to design a functional memory model and validate its read/write operations through a structured verification environment
